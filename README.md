# Campus Events — Static Website

## Overview
A small static site for showcasing campus events. Built with Parcel and served in Docker (nginx).

## TL responsibilities (high level)
- Create repo, `.dockerignore`, `Dockerfile`, initial commit and invite collaborators.
- Create branch protection rules: develop & main, PR approvals as described.
- After merging all features, build final image and push to TL's Docker Hub repo.

## Local dev (for contributors)
1. Clone repo:
   ```bash
   git clone <repo-url>
   cd campus-events